/target
/target_test_tmp/
/logs/
/tmp/

# 実行副産物（各ターンに自動再生成される events ビューア。追跡しない。#711）
/monitor/context_monitor.html

# ローカル実行設定（秘密情報を含むことがある。ひな形は config.json.sample）
/config/config.json

# ローカル固有ファイル（機密・マシン特定。共有除外）
/.cursor/
/AGENTS.md
/test_ollama.sh
