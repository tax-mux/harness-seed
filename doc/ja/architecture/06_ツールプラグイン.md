# ツールプラグインとパッケージング


実行層が使える道具を、プロセス内のプラグインとして登録し、設定のパック名でまとめて有効化する仕組みである。ツール実装がループ本体に散ると組み替えとテストが重いので、足し引きできる形にしている。ホストのドメイン API をツール以外の経路でエンジンに密結合しない。

組み込み集合を変えたい、独自ツールを足したいときに読む。既存パックのまま個別ツールの引数だけ知りたいなら [builtin_tools/README.md](../builtin_tools/README.md) で足りる。

用語: [用語集.md](用語集.md) · [EN](../../en/architecture/06_tool-plugins.md)

## 構成

| モジュール | 役割 |
|-----------|------|
| `src/tool/traits.rs` | `Tool` trait、`ToolContext` |
| `src/tool/registry.rs` | `ToolRegistry` — 登録・実行・カタログ |
| `src/tool/pack.rs` | `ToolPack` — 束ね登録 |
| `src/tool/builtin.rs` | 組み込み実装 |
| `src/tool/mod.rs` | `ToolRuntime` — invoke_id + レジストリ |

## ツールパック

| パック ID | ツール |
|-----------|--------|
| `basic` | `echo`, `time` |
| `coding` | `list_dir`, `grep`, `read_file`, `write_file`, `run_cmd` |
| `web_search` | `web_search`（Brave API キー必須） |
| `full` | 上記すべて（キーあり時に web 含む） |

## 設定

`config.json` の `tools.packs`（スイッチ形式）:

```json
{
  "tools": {
    "packs": {
      "basic": true,
      "coding": true,
      "web_search": false
    },
    "brave_search": { "api_key": "..." }
  }
}
```

- **未設定 / `{}`**: `basic` + `coding`。Brave キーがあれば `web_search` を自動追加。
- **明示スイッチ**: `true` のパックのみ有効。`web_search: false` で自動追加も抑止。
- **`full: true`**: 全パック一括（Brave キーあり時は web 含む）。
- 旧形式 `["basic", "coding"]` も後方互換で読める。

詳細: [config/README.md](../../../config/README.md)

## プロンプト連携

- `ToolRuntime::catalog()` → `PromptBlocks.tool_catalog`
- `TurnPromptContext` の system に動的カタログを載せる
- `web_search_enabled` はレジストリに `web_search` があるかで決まる

## ホストからの拡張

```rust
let mut rt = ToolRuntime::with_packs(env, brave, &[ToolPack::Basic, ToolPack::Coding]);
rt.register_plugin(Box::new(MyCustomTool));
blocks.tool_catalog = rt.catalog();
```

タスク定義の必須 `method` がレジストリに無い場合は、計画解決で自由記述へ落ちる（[task-registry.md](05_タスクレジストリ.md)）。

## 未実装（ideas）

| 項目 | メモ |
|------|------|
| パックの外部定義（toml / dylib / WASM） | 将来 |
| MCP → `Tool` ラップ | [ideas/tool-attention-reuse-ideas.md](../ideas/tool-attention-reuse-ideas.md) と併用想定 |

## 関連

- [architecture/02-01_ツールの選択.md](02-01_ツールの選択.md)
- [task-registry.md](05_タスクレジストリ.md)
